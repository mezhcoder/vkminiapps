"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.precisionRound = precisionRound;
exports.rescale = rescale;
exports.clamp = void 0;

var _toConsumableArray2 = _interopRequireDefault(require("@babel/runtime/helpers/toConsumableArray"));

var clamp = function clamp(value, min, max) {
  return Math.max(min, Math.min(value, max));
};

exports.clamp = clamp;

function precisionRound(number) {
  var precision = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
  var factor = Math.pow(10, precision);
  return Math.round(number * factor) / factor;
}

function rescale(value, from, to) {
  var options = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : {};
  var res = clamp.apply(void 0, [(value - from[0]) / (from[1] - from[0]) * (to[1] - to[0]) + to[0]].concat((0, _toConsumableArray2.default)(to)));
  var _options$step = options.step,
      step = _options$step === void 0 ? 0 : _options$step;

  if (step > 0) {
    var stepFloatPart = "".concat(step).split('.')[1] || '';
    return precisionRound(Math.round(res / step) * step, stepFloatPart.length);
  }

  return res;
}
//# sourceMappingURL=math.js.map