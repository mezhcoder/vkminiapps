import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
import { createScopedElement } from "../../lib/jsxRuntime";
import React from 'react';
import { getClassName } from "../../helpers/getClassName";
import { Icon24Spinner, Icon32Spinner, Icon44Spinner, Icon16Spinner } from '@vkontakte/icons';
import { usePlatform } from "../../hooks/usePlatform";

var svgSpinner = function svgSpinner(size) {
  switch (size) {
    case 'large':
      return createScopedElement(Icon44Spinner, {
        vkuiClass: "Spinner__self"
      });

    case 'medium':
      return createScopedElement(Icon32Spinner, {
        vkuiClass: "Spinner__self"
      });

    case 'small':
      return createScopedElement(Icon16Spinner, {
        vkuiClass: "Spinner__self"
      });

    default:
      return createScopedElement(Icon24Spinner, {
        vkuiClass: "Spinner__self"
      });
  }
};

var Spinner = function Spinner(_ref) {
  var size = _ref.size,
      restProps = _objectWithoutProperties(_ref, ["size"]);

  var platform = usePlatform();
  return createScopedElement("div", _extends({}, restProps, {
    vkuiClass: getClassName('Spinner', platform)
  }), svgSpinner(size));
};

Spinner.defaultProps = {
  size: 'regular'
};
export default /*#__PURE__*/React.memo(Spinner);
//# sourceMappingURL=Spinner.js.map