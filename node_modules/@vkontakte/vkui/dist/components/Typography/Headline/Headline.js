import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
import { createScopedElement } from "../../../lib/jsxRuntime";
import { usePlatform } from "../../../hooks/usePlatform";
import { classNames } from "../../../lib/classNames";
import { getClassName } from "../../../helpers/getClassName";
import { ANDROID } from "../../../lib/platform";

var Headline = function Headline(_ref) {
  var children = _ref.children,
      weight = _ref.weight,
      Component = _ref.Component,
      restProps = _objectWithoutProperties(_ref, ["children", "weight", "Component"]);

  var platform = usePlatform();
  var HeadlineComponent = Component;

  if (!Component) {
    HeadlineComponent = platform === ANDROID ? 'h3' : 'h4';
  }

  var headlineWeight = weight;

  if (platform === ANDROID && weight === 'semibold') {
    headlineWeight = 'medium';
  }

  return createScopedElement(HeadlineComponent, _extends({}, restProps, {
    vkuiClass: classNames(getClassName('Headline', platform), "Headline--w-".concat(headlineWeight))
  }), children);
};

export default Headline;
//# sourceMappingURL=Headline.js.map