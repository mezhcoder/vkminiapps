import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
import { createScopedElement } from "../../../lib/jsxRuntime";
import { usePlatform } from "../../../hooks/usePlatform";
import { classNames } from "../../../lib/classNames";
import { getClassName } from "../../../helpers/getClassName";
import { ANDROID } from "../../../lib/platform";

var Caption = function Caption(_ref) {
  var children = _ref.children,
      weight = _ref.weight,
      level = _ref.level,
      caps = _ref.caps,
      Component = _ref.Component,
      restProps = _objectWithoutProperties(_ref, ["children", "weight", "level", "caps", "Component"]);

  var platform = usePlatform();
  var captionWeight = weight;

  if (platform === ANDROID) {
    if (weight === 'semibold') {
      captionWeight = 'medium';
    }
  }

  return createScopedElement(Component, _extends({}, restProps, {
    vkuiClass: classNames(getClassName('Caption', platform), "Caption--w-".concat(captionWeight), "Caption--l-".concat(level), {
      'Caption--caps': caps
    })
  }), children);
};

Caption.defaultProps = {
  Component: 'div'
};
export default Caption;
//# sourceMappingURL=Caption.js.map