import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
import { createScopedElement } from "../../../lib/jsxRuntime";
import { usePlatform } from "../../../hooks/usePlatform";
import { classNames } from "../../../lib/classNames";
import { getClassName } from "../../../helpers/getClassName";
import { ANDROID } from "../../../lib/platform";

var Subhead = function Subhead(_ref) {
  var children = _ref.children,
      weight = _ref.weight,
      Component = _ref.Component,
      restProps = _objectWithoutProperties(_ref, ["children", "weight", "Component"]);

  var platform = usePlatform();
  var SubheadComponent = Component;
  var subheadWeight = platform === ANDROID && weight === 'semibold' ? 'medium' : weight;

  if (!Component) {
    SubheadComponent = platform === ANDROID ? 'h4' : 'h5';
  }

  return createScopedElement(SubheadComponent, _extends({}, restProps, {
    vkuiClass: classNames(getClassName('Subhead', platform), "Subhead--w-".concat(subheadWeight))
  }), children);
};

export default Subhead;
//# sourceMappingURL=Subhead.js.map