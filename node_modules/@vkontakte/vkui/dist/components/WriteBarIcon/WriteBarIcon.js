import _extends from "@babel/runtime/helpers/extends";
import _defineProperty from "@babel/runtime/helpers/defineProperty";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
import { createScopedElement } from "../../lib/jsxRuntime";
import { Icon24Send, Icon28AddCircleOutline, Icon28AttachOutline, Icon28CheckCircleOutline, Icon48WritebarDone, Icon48WritebarSend } from '@vkontakte/icons';
import { usePlatform } from "../../hooks/usePlatform";
import { classNames } from "../../lib/classNames";
import { getClassName } from "../../helpers/getClassName";
import { IOS } from "../../lib/platform";
import Caption from "../Typography/Caption/Caption";
export var WriteBarIcon = function WriteBarIcon(props) {
  var _classNames;

  var platform = usePlatform();

  var mode = props.mode,
      children = props.children,
      count = props.count,
      restProps = _objectWithoutProperties(props, ["mode", "children", "count"]);

  var childrenResolved;

  switch (mode) {
    case 'attach':
      childrenResolved = platform === IOS ? createScopedElement(Icon28AddCircleOutline, null) : createScopedElement(Icon28AttachOutline, null);
      break;

    case 'send':
      childrenResolved = platform === IOS ? createScopedElement(Icon48WritebarSend, null) : createScopedElement(Icon24Send, null);
      break;

    case 'done':
      childrenResolved = platform === IOS ? createScopedElement(Icon48WritebarDone, null) : createScopedElement(Icon28CheckCircleOutline, null);
      break;

    default:
      childrenResolved = children;
  }

  return createScopedElement("button", _extends({}, restProps, {
    vkuiClass: classNames(getClassName('WriteBarIcon', platform), (_classNames = {}, _defineProperty(_classNames, "WriteBarIcon--".concat(mode), !!mode), _defineProperty(_classNames, 'WriteBarIcon--disabled', restProps.disabled), _classNames))
  }), childrenResolved, count ? createScopedElement("div", {
    vkuiClass: "WriteBarIcon__count"
  }, createScopedElement(Caption, {
    weight: "regular",
    level: "2"
  }, count)) : null);
};
//# sourceMappingURL=WriteBarIcon.js.map